(function(){

    var x = 100;
    var y = 200;


    return 1000;
    // if naziv == 1000;
    // if naziv2 == 2000;

    // select sysdate from dual where id in (100,200,300);

    // Ako je tome tako onda to bas i nema previse smisla

})();